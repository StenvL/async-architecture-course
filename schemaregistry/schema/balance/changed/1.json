{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "balance.changed.v1",
  "description": "json schema for balance changed event (version 1)",
  "definitions": {
    "event_data": {
      "type": "object",
      "properties": {
        "account_id": {
          "type": "integer"
        },
        "balance_changing": {
          "type": "string"
        },
        "timestamp": {
          "type": "string"
        }
      },
      "required": [
        "account_id",
        "balance_changing",
        "timestamp"
      ]
    }
  },

  "type": "object",

  "properties": {
    "event_id":      { "type": "string" },
    "event_version": { "enum": [1] },
    "event_name":    { "type": "string" },
    "event_time":    { "type": "string" },
    "producer":      { "type": "string" },

    "data": { "$ref": "#/definitions/event_data" }
  },

  "required": [
    "event_id",
    "event_version",
    "event_name",
    "event_time",
    "producer",
    "data"
  ]
}